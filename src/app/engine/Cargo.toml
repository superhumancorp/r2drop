# engine/Cargo.toml — Rust workspace root for R2Drop
# Contains three crates: r2-core (shared logic), r2-ffi (C bridge), r2-cli (CLI binary)

[workspace]
members = ["r2-core", "r2-ffi", "r2-cli"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
